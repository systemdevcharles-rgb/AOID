# Block all direct HTTP access to uploaded files.
# Files are served through the authenticated Laravel route only.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Deny from all
</IfModule>
